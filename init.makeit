project: "MakeIt"

  version: "idk"
  language: "C"
  compiler: "gcc"
  build_dir: "$(DIR)/build"

sources = 
headers = 

search: "$(DIR)", "./src/*.h", headers

script: "$(DIR)/src/init.makeit"

library: "me"
library_dir: "external/lib"
include_dir: "external/include"

makefile: "", sources, headers
