project: "MakeIt"

  version: "idk"
  language: "C"
  compiler: "gcc"
  build_dir: "$(CURRENT_DIR)/build"

sources = 
headers = "$(CURRENT_DIR)/src/**.h"

script: "$(CURRENT_DIR)/src/init.makeit"

library: "me"
library_dir: "external/lib"
include_dir: "external/include"

makefile: "", sources, headers
